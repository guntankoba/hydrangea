# Hydrangea 謎解きアプリ 仕様書（v0.2.0）

本ドキュメントは Hydrangea（街歩き × 思い出 × 謎解き）アプリの仕様を一元管理する。
実装は原則として本仕様に準拠する。

---

## 1. 実装ルール

- 仕様書に **バージョン番号** を付与し、変更のたびに更新履歴を管理すること（例：v0.2.0）。
- **明確な指示なく、既に実装済みのコードを削除しないこと。** リファクタ時はコメント等で理由を残すこと。
- 本ドキュメントの **章立て・構成** を、明確な合意なしに勝手に変更しないこと。
- 隣に開発者がいる状態で解いてもらう運用とするため、**アプリ内でユーザーに伝わる文章によるヒントは出さないこと。**
- 画像等のデータが必要な場合は **サンプルデータで実装** し、実運用用データは開発者から受け取るフローとすること。
- アプリ側のステージ名は、ユーザーに答えが分からないように **「ステージ1」「ステージ2」** のようなシンプルな名称とし、場所名や答えにつながるワードを含めないこと。
- 上記ルールは今後必要に応じて追記・拡張すること。

---

## 2. 全体フロー

### 2.1 ルート概要
- **起点：自宅（金町）**
- **ゴール：椿山荘**

### 2.2 ステージ一覧

| ST  | 出発地   | 到着地   |
|-----|----------|----------|
| ST1 | 自宅     | 金町     |
| ST2 | 金町     | 秋葉原   |
| ST3 | 秋葉原   | 新宿     |
| ST4 | 新宿     | 目白     |
| ST5 | 目白     | 椿山荘   |

---

## 3. ステージ仕様（各ステージごとに作成）

各ステージは「区間」「目的」「構成」「入力仕様」「正解」「クリア後遷移」を最低限書く。
入力ルールは **問題ごとにここで定義する**（グローバル共通ルールは持たない）。

---

### 3.1 ST1 自宅 → 金町（例題ステージ）

- **区間**：自宅 → 金町
- **目的**：
  - 本編に入る前の「例題」として、ゲームの進め方（写真を見る → 最寄り駅名を入力する）を学んでもらう。
  - 誰でもほぼ確実に正解できる難易度とし、不安感を与えない。

#### 3.1.1 構成
- 公園の写真を 1 枚表示する。
- 問い：「この写真は下記の公園一覧のどこかです。この公園がある最寄り駅はどこですか？」を 1 問のみ出題する。
- 公園一覧は画面上にテキストとして表示する。
  - 表示する公園名（順不同）：
    1. 上野恩賜公園
    2. にいじゅくみらい公園
    3. 小石川後楽園
    4. 明治神宮外苑
    5. 水元公園
    6. 玉川上水第三公園
    7. ひだまり公園あやがわ
    8. 等々力渓谷公園
  - 色付けルール：
    - **にいじゅくみらい公園**については、「にいじゅく」の文字部分のみを ST3 で使用するアクセントカラー（例：`--color-accent-shinjuku`）でハイライトする。
    - 上記以外の公園については、これまでの仕様で使用していない新規カラーを、公園名の一部分（1〜2文字）にのみ適用する。
      - 例：
        - 上野恩賜公園：`上野` の「野」を新規カラー A でハイライト。
        - 小石川後楽園：`後楽園` の「楽」を新規カラー B でハイライト。
        - 明治神宮外苑：`外苑` の「外」を新規カラー C でハイライト。
        - 水元公園：`水元` の「水」を新規カラー D でハイライト。
        - 玉川上水第三公園：`玉川上水` の「玉」を新規カラー E でハイライト。
        - ひだまり公園あやがわ：`ひだまり` の「ひ」を新規カラー F でハイライト。
        - 等々力渓谷公園：`等々力` の「等」を新規カラー G でハイライト。
      - 具体的なカラーコードはデザイン段階で決定し、既存のアクセントカラー（山手線カラー、`--color-accent-shinjuku`、路線カラーなど）と重複しない値を採用すること。
- 画像ファイル名（仮）：`st1_example_park_kanamachi.png`
  - `alt: ""`（答えに関する情報は含めない）。

#### 3.1.2 入力仕様

- テキストボックス 1 つ。
- 入力例：`かなまち` / `金町`。
- 実装側で行う正規化：
  - 前後スペースをトリム。
  - 全角・半角の違いは無視。

#### 3.1.3 正解
- ひらがな：`かなまち`
- 漢字：`金町`

#### 3.1.4 クリア後遷移

- 正解入力後、金町の StationCard（`id: "kanamachi"`）を画面に表示する。
  - 表示内容の例：駅名「金町」、路線カラー（JR 常磐緩行線の lineColor）、数値 21。
- StationCard の表示が完了したら、ST2（金町 → 秋葉原）へ遷移する。

---



### 3.2 ST2 金町 → 秋葉原（思い出 5 問＋時系列ソート）

- **区間**：金町 → 秋葉原
- **目的**：
  - 実際の思い出（デート・旅行・宿・ラーメン・出会いアプリ）を題材にした 5 問を解き、それぞれから 1 文字ずつ取得する。
  - 各問の文字カードには、対応する出来事の日付（`dateISO`）を紐づけて保存し、FINAL で「古い日付 → 新しい日付」に並べ替える。
  - 抽出した 5 文字を連結して **`あきはばら`** を作り、最終的な目的地「秋葉原」に到達する。

---

#### 3.2.1 構成（概要）

- 小問 **Q1〜Q5（ST2_Q1〜ST2_Q5）** を固定順（Q1→Q5）で出題する。
  - 各問では、特定の思い出を想起させる質問に答えることで「ことばの一部（駅名や場所名など）」を導き、その中から 1 文字を取得する。
  - 取得する文字：**あ／き／は／ば／ら**（5 文字）。
- 最後に **ST2_FINAL** として、5 枚のカードを**実体験の日付順（古い→新しい）**に並べ替え、抽出枠（指定色）の文字だけを連結して入力させる。
  - 正解：`あきはばら` または `秋葉原`。

---

#### 3.2.2 ST2 共通仕様（入力・保存）

- **入力方式（共通）**
  - 全ての小問（Q1〜Q5）は **ひらがな入力のみ** を受け付ける。
    - カタカナが入力された場合は自動でひらがなに変換して判定する。
- **スロット表示**
  - 1 文字 = 1 枠の**等幅ボックス**で表示する。
  - 各問には「あ・き・は・ば・ら」のいずれかに対応する**ターゲット枠**があり、その背景色を `#0d221e` とする。
    - UI トークン例：`--slot-target-bg: #0d221e`。
  - ターゲット枠の文字色は **白 (#FFFFFF)** とする（UIトークン例：`--slot-fg-dark: #FFFFFF`）。
  - ターゲット枠には**下線**も付与し、色覚多様性に配慮する。
- **事前入力（ダミー文字）**
  - 各問のスロットには、**「あきはばら」に含まれないひらがな 1 文字**を、ランダムで 1 枠だけ事前入力しておく。
    - 例：Q1 では `◯ぞ◯◯` のように 1 枠だけ「ぞ」を埋めておく。
- **保存仕様（LetterCard）**
  - 各問をクリアした時点で、次のようなオブジェクトを配列 `letters` に追加する：

    ```ts
    letters.push({ id: 'ST2_Qx_*', letter: '*', memoryTitle: '*', dateISO: '{DATE_*}' });
    ```

  - `letter` には取得した 1 文字（あ／き／は／ば／ら）、`memoryTitle` には思い出の短いタイトル、`dateISO` には実際の体験日を `YYYY-MM-DD` 形式で格納する。

---

#### 3.2.3 小問仕様（Q1〜Q5）

##### ST2_Q1｜き（出典：謎解き）
- **id**：`ST2_Q1_KI`
- **タイトル**：初めてのデートで最後にした遊びは？
- **想定する回答語**：`なぞとき`
- **回答欄（UI仕様）**：
  - スロット数：4（「な・ぞ・と・き」）
  - 表示例：`◯ぞ◯◯`
    - ターゲット枠：**4番目の枠**（文字「き」）の背景を `#0d221e`、文字色を白にする。
    - 事前入力例：2番目の枠に「あきはばら」に含まれない文字（例：「ぞ」）。
  - 入力制約：ひらがなのみ。
- **取得文字**：`き`
- **クリア時保存**：
  - `letters.push({ id: 'ST2_Q1_KI', letter: 'き', memoryTitle: 'ことばの終わり', dateISO: '{DATE_K}' });`

##### ST2_Q2｜は（出典：箱根）
- **id**：`ST2_Q2_HA`
- **タイトル**：初めての旅行で行った観光地は？
- **想定する回答語**：`はこね`
- **回答欄（UI仕様）**：
  - スロット数：3（「は・こ・ね」）
  - 表示例：`◯◯◯`
    - ターゲット枠：**1番目の枠**（文字「は」）の背景を `#0d221e`、文字色を白にする。
    - 事前入力：3 枠のうち 1 枠に「あきはばら」に含まれないランダム文字を事前入力。
  - 入力制約：ひらがなのみ。
- **取得文字**：`は`
- **クリア時保存**：
  - `letters.push({ id: 'ST2_Q2_HA', letter: 'は', memoryTitle: '箱根のはじまり', dateISO: '{DATE_HA}' });`

##### ST2_Q3｜ば（出典：馬喰町）
- **id**：`ST2_Q3_BA`
- **タイトル**：初めて化粧品をもらった■の宿がある最寄りの駅名は？（■ = 色 `#0d221e`）
- **想定する回答語**：`ばくろちょう`
- **回答欄（UI仕様）**：
  - スロット数：5（「ば・く・ろ・ちょ・う」）
  - 表示例：`◯◯◯◯◯`
    - ターゲット枠：**1番目の枠**（文字「ば」）の背景を `#0d221e`、文字色を白にする。
    - 事前入力：5 枠のうち 1 枠に「あきはばら」に含まれないランダム文字を事前入力。
  - 入力制約：ひらがなのみ。
- **取得文字**：`ば`
- **クリア時保存**：
  - `letters.push({ id: 'ST2_Q3_BA', letter: 'ば', memoryTitle: '馬喰町の最初', dateISO: '{DATE_BA}' });`

##### ST2_Q4｜ら（出典：みうらや）
- **id**：`ST2_Q4_RA`
- **タイトル**：初めて今いる駅近くで食べた家系ラーメン屋は？
- **想定する回答語**：`みうらや`
- **回答欄（UI仕様）**：
  - スロット数：4（「み・う・ら・や」）
  - 表示例：`◯◯◯◯`
    - ターゲット枠：**3番目の枠**（文字「ら」）の背景を `#0d221e`、文字色を白にする。
    - 事前入力：4 枠のうち 1 枠に「あきはばら」に含まれないランダム文字を事前入力。
  - 入力制約：ひらがなのみ。
- **取得文字**：`ら`
- **クリア時保存**：
  - `letters.push({ id: 'ST2_Q4_RA', letter: 'ら', memoryTitle: 'みうらやの3文字目', dateISO: '{DATE_RA}' });`

##### ST2_Q5｜あ（出典：Omiai）
- **id**：`ST2_Q5_A`
- **タイトル**：初めて知り合ったアプリは？
- **想定する回答語**：`おみあい`
- **回答欄（UI仕様）**：
  - スロット数：4（「お・み・あ・い」）
  - 表示例：`◯◯◯◯`
    - ターゲット枠：**3番目の枠**（文字「あ」）の背景を `#0d221e`、文字色を白にする。
    - 事前入力：4 枠のうち 1 枠に「あきはばら」に含まれないランダム文字を事前入力。
  - 入力制約：ひらがなのみ。
- **取得文字**：`あ`
- **クリア時保存**：
  - `letters.push({ id: 'ST2_Q5_A', letter: 'あ', memoryTitle: 'おみあいの三文字目', dateISO: '{DATE_A}' });`

---

#### 3.2.4 FINAL：時系列並べ替え → 連結 → 漢字昇格

- **id**：`ST2_FINAL_TIMELINE`
- **タイトル**：時系列で ならべて こたえを作れ
- **UI 構成**：
  - **カード表示**：
    - Q1〜Q5 の回答語を、それぞれの問と同じスロット表示で再掲する。
    - 各カード内のターゲット枠は背景 `#0d221e`／文字色 `#FFFFFF`（抽出対象）とする。
    - 例：Q1（なぞとき）→ `◯ぞ◯◯`（最後の枠がターゲット色）。
  - **ドラッグソート**：
    - 5 枚のカードを、思い出の日付（`dateISO`）にもとづき**古い日付 → 新しい日付**の順に並び替える UI。
  - **入力欄**：
    - 1 行テキスト入力欄。
    - 背景 `#0d221e`／文字色 `#FFFFFF`（例：`--final-input-bg: #0d221e; --final-input-fg: #FFFFFF`）。

- **指示テキスト（趣旨）**：
  - 「■色（#0d221e）を目印に、カードを古い日付から新しい日付の順に並べ替えよ。」
  - 「並べ替えた順番で、各カードの■色の枠の文字だけを抜き出し、下の欄に連結して入力せよ。」

- **入力と判定**：
  - 入力：連結した語（ひらがな 5 文字、または漢字）
    - 期待解：`あきはばら` **または** `秋葉原`。
  - 判定ロジック（概念）：
    - 並べ替え後の `letters` を `dateISO` 昇順で評価し、抽出対象の 5 文字が「あ・き・は・ば・ら」となる並びを正解とする。
    - 入力値を正規化し、`"あきはばら"` または `"秋葉原"` と一致すれば正解。

- **正解時の演出（概要）**：
  - 抽出された 5 文字が `あきはばら` になったタイミングで、スロットやテキストを強調表示する（点滅・拡大等）。
  - 「ルートが見えた。次へ進もう。」といったメッセージを表示する。

---

#### 3.2.5 入力仕様（ST2 全体）

- 小問 Q1〜Q5：
  - ひらがな入力のみ。
  - カタカナ・全角／半角・前後スペースなどは実装側で正規化してから判定する。
- FINAL：
  - ひらがな `あきはばら`、または漢字 `秋葉原` を正解として受け付ける。

#### 3.2.6 正解

- 最終的な答え：
  - ひらがな：`あきはばら`
  - 漢字：`秋葉原`

#### 3.2.7 クリア後遷移

- FINAL で正解入力後、秋葉原の StationCard（`id: "akihabara"`）を画面に表示する。
  - 表示内容の例：駅名「秋葉原」、路線カラー（山手線の lineColor）、数値 3。
- StationCard の表示が完了したら、ST3（秋葉原 → 新宿）へ遷移する。

---

### 3.3 ST3 秋葉原 → 新宿（色×動物看板と変換規則）

- **区間**：秋葉原 → 新宿
- **目的**：
  - 秋葉原周辺を実際に街歩きし、「色 × 動物」の看板を見つけてもらう。
  - Q1〜Q4 を通じて ①〜④ の文字を導き、変換規則
    - ① → ②（に → し）
    - ③ → ④（い → ん）
    を完成させる。
  - ST1 の問題文に登場した「にいじゅくみらい公園」の「にいじゅく」（🔵背景）にこの規則を適用し、
    `にいじゅく → しんじゅく` と変換することで、次に向かう駅が新宿であると気づかせる。

#### 3.3.1 FINAL：変換規則の提示
- ステージ開始時点で、以下の穴あき規則を表示しておく：
  - ① → ②
  - ③ → ④
- 併せて次の指示を表示：
  - 「下記の規則に従って、『にいじゅくみらい公園』の『にいじゅく』（🔵背景）を変えろ」
- 🔵：ST1 の問題文中、「にいじゅく」の文字背景が青くハイライトされていることを示す。
- ①〜④ は最初は空欄／スロット表示にしておき、Q1〜Q4 の解答により埋まっていく。

#### 3.3.2 Q1〜Q3：看板から ①〜③ を導く

共通仕様：
- それぞれ「色 × 動物」を特徴とする看板の写真を表示し、店名をひらがなで入力させる。
- 画像はサンプルで実装し、実際の写真は後で差し替える。

例：
- **Q1**：白猫と向き合う赤牛
  - 問：`白猫と向き合う赤牛　〇〇①〇〇〇〇〇〇`
  - 答：`やきにくのまんせい`
  - 3文字目 → ① = `に`
- **Q2**：緑のたぬき
  - 問：`緑のたぬき　〇〇〇②〇〇〇〇〇〇〇`
  - 答：`かーどしょっぷぽんぽこ`
  - 4文字目 → ② = `し`
- **Q3**：猫と鶏が隣り合う青い方
  - 問：`猫と鶏が隣り合う青い方　〇③〇〇〇〇〇`
  - 答：`はいからちきん`
  - 2文字目 → ③ = `い`

#### 3.3.3 Q4：①〜③ から ④ を導く
- 問：`Q1 の 7 文字目、Q2 の 9 文字目、Q3 の 7 文字目`
- 各解答から指定位置の文字を取り出すと、すべて `ん` になる。
  - よって ④ = `ん`
- 答：`ん`

#### 3.3.4 変換規則完成〜最終入力
- 完成した規則：
  - `に → し`
  - `い → ん`
- ST1 の「にいじゅく」に規則を適用：
  - `にいじゅく` → `しんじゅく`
- ユーザーに「変換後の駅名」を入力させる。
  - 正解：`しんじゅく` / `新宿`

#### 3.3.5 クリア後遷移

- 「しんじゅく／新宿」に正解後、新宿の StationCard（`id: "shinjuku"`）を画面に表示する。
  - 表示内容の例：駅名「新宿」、路線カラー（山手線の lineColor）、数値 17。
- StationCard の表示が完了したら、ST4（新宿 → 目白）へ遷移する。

---



### 3.4 ST4 新宿 → 目白（駅ナンバリングパズルで 14 → JY14 → 目白）

- **区間**：新宿 → 目白
- **目的**：
  1. 駅ナンバリング（路線記号＋数字）をモチーフにした数式パズルを解くことで、まずは数値 **14** を導出させる。
  2. 導き出した 14 を「JY14」という山手線の駅ナンバリングと結びつけ、**JY14 = 目白** に自力で気づいてもらう。
  3. JY 系列であることを自然に察してもらうために、**最終回答欄の背景色** と、ST2・ST3 に登場する山手線の駅名（秋葉原・新宿）の表示色を、共通して「山手線カラー」にしておく。

#### 3.4.1 ステージ構成

- **フェーズ1：駅クイズ（駅ナンバリングの取得）**
  - 下北沢 / 下高井戸 / 片瀬江ノ島 / 豊島園 / 蔵前 の 5 駅について、それぞれ 1 問ずつ駅名クイズを出題する。
  - すべて「画像に写っているものを一緒に飲食した駅はどこ？」という形式とする。
  - 共通問題文イメージ：
    - 「この写真に写っているものを一緒に飲食した駅はどこ？」
  - 各クイズで駅名を正しく答えると、その駅の駅ナンバリングと数値部分を持つ StationCard を獲得する。
  - 画像ファイル名（仮）：

    | 問い | 駅名       | 画像ファイル名（仮）              |
    |------|------------|------------------------------------|
    | Q1   | 下北沢     | `st4_q1_shimokitazawa.png`        |
    | Q2   | 下高井戸   | `st4_q2_shimotakaido.png`         |
    | Q3   | 片瀬江ノ島 | `st4_q3_katase_enoshima.png`      |
    | Q4   | 豊島園     | `st4_q4_toshimaen.png`            |
    | Q5   | 蔵前       | `st4_q5_kuramae.png`              |

- **フェーズ2：数式パズル**
  - フェーズ1で獲得した StationCard を用いて、次の式を完成させる：
    - `□1 × □2 − □3 − □4 ＋ □5 = 14`
  - 正解となる駅の配置：
    - 下北沢（7）× 下高井戸（7） − 片瀬江ノ島（16） − 豊島園（36）＋ 蔵前（17） = 14

- **入力仕様**
  - 駅クイズ部分：駅名は漢字入力を基本とし、スペースや全角／半角の違いは実装側で正規化。
  - 数式フェーズ：ドラッグ＆ドロップまたは選択式で StationCard をスロットに配置し、数値の直接入力はさせない。

- **正解条件**
  - 上記の組み合わせで数式が 14 になったときにフェーズ2クリアとする。
  - その後、14 を「JY14」とみなし、最終的に「目白」と入力できた時点で ST4 をクリアと判定する。

- **クリア後遷移**
  - 「目白／めじろ」に正解後、目白の StationCard（`id: "mejiro"`）を画面に表示する。
    - 表示内容の例：駅名「目白」、路線カラー（山手線の lineColor）、数値 14。
  - StationCard の表示が完了したら、ST5（目白 → 椿山荘）へ遷移する。

---

---------|----------------|--------|----------|
| 金町       | JR常磐緩行線   | JL21   | 21       |
| 秋葉原     | JR山手線       | JY03   | 3        |
| 新宿       | JR山手線       | JY17   | 17       |
| 目白       | JR山手線       | JY14   | 14       |
| 下北沢     | 小田急小田原線 | OH07   | 7        |
| 下高井戸   | 京王線         | KO07   | 7        |
| 片瀬江ノ島 | 小田急江ノ島線 | OE16   | 16       |
| 豊島園     | 都営大江戸線   | E36    | 36       |
| 蔵前       | 都営浅草線     | A17    | 17       |

※ 新たな駅を問題に登場させる場合は、駅ナンバリングを確認のうえ、この一覧に追記すること。

---

#### 3.4.4 StationCard の仕様（共通）

本アプリでは、駅に関する情報を UI 上でカードとして扱うために **StationCard** を用いる。
StationCard は ST1〜ST4 のクリア時および ST4 の数式パズルで使用する共通フォーマットとする。

##### StationCard 型イメージ

```ts
interface StationCard {
  id:
    | "kanamachi"
    | "akihabara"
    | "shinjuku"
    | "mejiro"
    | "shimokitazawa"
    | "shimotakaido"
    | "katase-enoshima"
    | "toshimaen"
    | "kuramae";
  stationName: string; // 画面表示用の駅名（例："下北沢"）
  lineColor: string;   // 路線カラー（HEX 値、lineColor に対応）
  value: number;       // 駅ナンバリングの数値部分のみ（例：7, 16, 36, 17, 21, 3, 14）
}
```

- `stationName` は日本語の正式な駅名とする（例：`"金町"`）。
- `lineColor` は路線カラーを表す HEX 値であり、カラー仕様（9章）の `lineColor` に対応させる。
- `value` は駅ナンバリングの数値部分のみを整数として持つ（例：JY14 → 14）。

##### StationCard 一覧（現時点で使用する駅）

| id              | stationName | value | lineColor | 備考                                      |
|-----------------|-------------|-------|----------|-------------------------------------------|
| kanamachi       | 金町        | 21    | #00A0DE  | JR 常磐緩行線（joban-local）              |
| akihabara       | 秋葉原      | 3     | #9ACD32  | JR 山手線（yamanote）                     |
| shinjuku        | 新宿        | 17    | #9ACD32  | JR 山手線（yamanote）                     |
| mejiro          | 目白        | 14    | #9ACD32  | JR 山手線（yamanote）                     |
| shimokitazawa   | 下北沢      | 7     | #007ED5  | 小田急小田原線（odakyu-main）            |
| shimotakaido    | 下高井戸    | 7     | #E7007E  | 京王線（keio）                            |
| katase-enoshima | 片瀬江ノ島  | 16    | #007ED5  | 小田急江ノ島線（odakyu-enoshima）        |
| toshimaen       | 豊島園      | 36    | #CE045B  | 都営大江戸線（toei-oedo）                |
| kuramae         | 蔵前        | 17    | #EC6E65  | 都営浅草線（toei-asakusa）               |

- 追加の駅に対して StationCard を定義する場合は、この表に `id / stationName / value / lineColor / 備考` を追記すること。

---

--------------|-----------|------------------------------|
| shimokitazawa   | #007ed5   | 小田急線系のラインカラー    |
| shimotakaido    | #e7007e   | 京王線ラインカラー          |
| katase-enoshima | #007ed5   | 小田急江ノ島線              |
| toshimaen       | #ce045b   | 都営大江戸線                |
| kuramae         | #ec6e65   | 都営浅草線                  |

- 上記以外の StationCard を追加する場合は、同様に `id / lineColor / 備考` をこの表に追記すること。

---

---------|----------------|--------|----------|
| 金町       | JR常磐緩行線   | JL21   | 21       |
| 秋葉原     | JR山手線       | JY03   | 3        |
| 新宿       | JR山手線       | JY17   | 17       |
| 目白       | JR山手線       | JY14   | 14       |
| 下北沢     | 小田急小田原線 | OH07   | 7        |
| 下高井戸   | 京王線         | KO07   | 7        |
| 片瀬江ノ島 | 小田急江ノ島線 | OE16   | 16       |
| 豊島園     | 都営大江戸線   | E36    | 36       |
| 蔵前       | 都営浅草線     | A17    | 17       |

---

### 3.5 ST5 目白 → 椿山荘（最終ステージ：白61 に乗るための番号）

- **区間**：目白 → 椿山荘
- **目的**：
  - これまでに辿ってきた駅（ST1〜ST4）の "駅番号の数値部分" を振り返らせ、その合計から最終的なバス番号 **「白61」** を導出させる。
  - アプリ上では **文章だけの 1 問** で構成し、シンプルだがこれまでの伏線（駅ナンバリング・数字）を回収するクライマックスとする。
  - 実際の運用では、当日作者が身に着ける腕時計（「時を刻まないもの」）に欠けている数字 6 を気づかせ、合計 55 に 6 を足して 61 に到達させる。

---

#### 3.5.1 構成

- ST5 では、新たな画像や複数の小問は用意せず、**文章だけの 1 問** を出題する。
- プレイヤーは、これまで通ってきた駅の「駅番号の数値部分」を思い出し、その合計値をまず算出する。
  - 想定している駅と数値部分：
    - 金町：JL21 → 21
    - 秋葉原：JY03 → 3
    - 新宿：JY17 → 17
    - 目白：JY14 → 14
  - 合計：21 + 3 + 17 + 14 = **55**
- 次に、「時を刻まないもの（腕時計）」に欠けている刻印の数字 6 を見抜き、55 に加算して **61** を導く。
- 導かれた 61 を、目白駅から椿山荘方面へ向かうバス路線「白61」の番号と結びつける。

> 補足：
> - 「時を刻まないもの」は、当日作者が身に着ける腕時計（動いていない／針がない／装飾目的など）を指す。
> - 腕時計の文字盤から「6」の刻印だけが欠けている状態を想定し、プレイヤーは実際にそれを見て「欠けている刻印の数字 = 6」に気づく。
> - アプリ側では画像などのヒントは出さず、問題文のみを提示する（実装ルールに従う）。

---

#### 3.5.2 問題文

アプリに表示する問題文は以下とする：

> 今日ふたりが辿ってきた道の数字をすべて足し合わせよ。
>
> その合計に、
> “時を刻まないもの” に欠けている刻印の数字を加えよ。
>
> 導かれた数が、次に進むための番号となる。

- 問題文内では「バス」「白61」「椿山荘」などの直接的な語は出さない。
- 「次に進むための番号」が、実際には「白61」の "61" を意味している。

---

#### 3.5.3 入力仕様と正解

- **入力形式**：
  - 数字入力（テキストボックス 1 つ）。
  - 半角・全角どちらでも受け付けるが、内部では数値にパースして扱う。

- **正解**：
  - `61`

- **クリア後遷移**：
  - 正解後、画面上で「目白駅から『白61』に乗る」ことを示唆するテキスト（具体的な乗り場案内などは別途検討）を表示し、物理的な移動先として椿山荘をゴールに案内する。

---



## 4. UI / 画面構成

### 4.1 画面一覧

- トップ画面
- 利用規約（偽）画面（PRE_TOS）
- ステージ画面（ステージ1〜ステージ5）
- クリア画面（全ステージクリア後）

### 4.2 画面遷移

基本フローは直列だが、**一度解いたステージについては行き来できるナビゲーション**を提供する。

#### 4.2.1 基本フロー

```text
トップ
  ↓
利用規約（偽）
  ↓
ステージ1（ST1 自宅 → 金町）
  ↓
ステージ2（ST2 金町 → 秋葉原）
  ↓
ステージ3（ST3 秋葉原 → 新宿）
  ↓
ステージ4（ST4 新宿 → 目白）
  ↓
ステージ5（ST5 目白 → 椿山荘）
  ↓
クリア画面
```

#### 4.2.2 ステージ間の移動仕様

- ステージ一覧画面（またはヘッダーのメニュー）から、**既にクリア済みのステージ（ST1〜ST5）には自由に前後移動できる。**
  - 例：ST3 までクリア済みの場合、ステージ1〜3 は任意に開くことができるが、ステージ4 以降はロック表示とする。
- 途中のステージに戻って内容を見直しても、クリア状況（最後にクリアしたステージ番号）は変わらない。
- ロック状態のステージはタップできないか、「まだ進めない」旨のメッセージのみ表示する。

#### 4.2.3 ステージ内の前後移動

- ST2〜ST4 のように複数の小問やフェーズを持つステージでは、**一度クリアした小問・フェーズ間を前後移動できる** UI を用意する（例：「前へ」「次へ」ボタン）。
- 過去の小問画面を再度開いても、そのステージのクリア済み状態や全体の進行には影響しない（あくまで「見直し用」）。

- 画面上のステージ名表示は「ステージ1〜5」のようなシンプルな名称とし、場所名は UI には出さない（実装ルール参照）。

### 4.3 入力ルールについて

- 共通のグローバル入力ルールは定義せず、**各問題ごとに第3章で仕様を記載する**。
- 追加の入力形式（スロット、ドラッグ＆ドロップなど）を実装する場合は、対応する問題の仕様に明記する。

---

## 5. データ構造（現行実装ベース）

- 現行実装では、`src/main.ts` の `puzzles` 配列を中心に謎解きロジックが構成されている。
- 各要素は「問題文」「答え」「ステージ番号」等を持つオブジェクトとなっている。
- フィールド構成・型の詳細は **コードをソース・オブ・トゥルース** とし、変更時には必ず本仕様書のバージョンを更新すること。

将来的に `Stage` / `Question` などの型に整理する場合：
- 先にコード側で型定義・データ構造を整備し、動作確認後に本仕様書へ新セクションとして追記する。

---

## 6. 再開 / 保存仕様

- ブラウザの `localStorage` に、各ステージのクリア状況（例：`clearedStages`）と、**最終クリアステージ番号（例：`maxClearedStage`）** を保存する。
- 再読み込み時：
  - `maxClearedStage` までのステージは「解放済み」とし、いつでも開ける。
  - `maxClearedStage + 1` のステージは「次に進むべきステージ」として開ける。
  - それより先のステージはロック表示とする。
- ステージ内の進行状態（どのフェーズまで到達したか）を追加で保存する場合は、ステージごとのキー（例：`stage3_progress`）で管理する。

---



## 7. バージョン管理

- v0.1.0：初版ドラフト
- v0.2.0：ST1〜ST4 の仕様整理、駅ナンバリング表、実装ルール明文化。

---

## 8. 用語集

- ST：ステージ（ST1〜ST5）。
- 駅ナンバリング：路線記号＋数字で表される駅番号（例：JY14）。
- 山手線カラー：`lineColor` の一種として定義する。山手線の路線カラー（例：`lineColor.yamanote = "#9ACD32"` のような黄緑）を指し、ST2〜ST4 で山手線の駅名や最終回答欄の背景色に用いる。


---

## 9. カラー仕様

### 9.1 カラー概念の整理

本アプリで扱うカラーは、大きく次の 2 系統に分けて扱う。

1. **路線カラー（`lineColor`）**
   - 実在する鉄道路線ごとのベースカラー。
   - 駅名ラベル、StationCard、路線を意識させたい UI 要素などに使用する。
   - 山手線カラーも `lineColor` の一つとして扱う。
2. **パズル用アクセントカラー**
   - 特定の文字や単語（例：ST1 の公園名の一部、ST3 の「にいじゅく」など）を強調するための色。
   - 実在の路線とは直接結びつかない「謎解き演出用」のカラー。
   - `lineColor` と混同しないよう、別のトークン名（例：`--color-accent-*`）で管理する。

### 9.2 `lineColor` の例

`lineColor` は「路線ごとのベースカラー」を表す。代表的な例を次に示す（必要に応じて拡張する）。

| lineId          | 路線名             | 用途の例                                           | HEX 値  |
|-----------------|--------------------|----------------------------------------------------|---------|
| yamanote        | JR 山手線          | 秋葉原・新宿・目白の駅名ラベル／最終回答欄など   | #9ACD32 |
| joban-local     | JR 常磐緩行線      | 金町の駅名ラベルなど                               | #00A0DE |
| odakyu-main     | 小田急小田原線     | 下北沢 StationCard の `lineColor`                 | #007ED5 |
| odakyu-enoshima | 小田急江ノ島線     | 片瀬江ノ島 StationCard の `lineColor`             | #007ED5 |
| keio            | 京王線             | 下高井戸 StationCard の `lineColor`               | #E7007E |
| toei-oedo       | 都営大江戸線       | 豊島園 StationCard の `lineColor`                 | #CE045B |
| toei-asakusa    | 都営浅草線         | 蔵前 StationCard の `lineColor`                   | #EC6E65 |

- StationCard の `lineColor` は、上記 `lineId` に対応する色を用いる。
- 山手線カラー（秋葉原・新宿・目白で使用する色）は、`lineColor.yamanote` として統一的に扱う。

### 9.3 パズル用アクセントカラー

- ST1 の公園一覧で用いる「新規カラー A〜G」や、ST3 で「にいじゅく」を強調する色は、**パズル用アクセントカラー**として扱う。
- これらは `lineColor` とは別物であり、以下のような方針で決める：
  - 路線カラー（`lineColor.*`）と HEX 値が被らない色を採用する。
  - 役割ベースのトークン名で管理する（例：`--color-accent-shinjuku`、`--color-accent-park-a` など）。
- 仕様書内で「山手線カラー」「路線カラー」と書かれている箇所は `lineColor` を指し、「アクセントカラー」はパズル演出用カラーを指すものとする。

